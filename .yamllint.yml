---
extends: default

rules:
  document-start:
    present: true
  empty-lines:
    max-end: 1
  line-length:
    max: 120
    level: warning
  truthy:
    allowed-values:
      - "true"
      - "false"
      - "on"
      - "off"
    check-keys: false

ignore: |
  charts/*/templates/
